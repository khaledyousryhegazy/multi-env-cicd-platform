apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app.kubernetes.io/name: backend-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend-deployment
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 4000
