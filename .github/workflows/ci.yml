name: Multi environment CI/CD pipeline
on:
  push:
    branches:
      - main
jobs:
  build-and-push-code:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.0

      - name: Setup Node.js environment
        uses: actions/setup-node@v6.4.0
        with:
          node-version: 24
          package-manager-cache: false
      - run: |
          cd app/frontend && npm i
          cd ../backend && npm i

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Docker Setup
        uses: docker/setup-buildx-action@v4.2.0

      - name: Build and push Docker frontend image
        uses: docker/build-push-action@v7.3.0
        with:
          context: app/frontend
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/quran_app-frontend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push Docker backend image
        uses: docker/build-push-action@v7.3.0
        with:
          context: app/backend
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/quran_app-backend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

# ======================================================================================#
# ====================MY FREE TIER AWS ENDED THAT WAS FOR ECR, EKS======================#
# ======================================================================================#

# name: Multi environment CI/CD pipeline
# on:
#   push:
#     branches:
#       - main
# jobs:
#   build-and-push-code:
#     runs-on: ubuntu-latest
#     permissions:
#       id-token: write
#       contents: read
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v7.0.0

#       - name: Setup Node.js environment
#         uses: actions/setup-node@v6.4.0
#         with:
#           node-version: 24
#           package-manager-cache: false
#       - run: |
#           cd app/frontend && npm i
#           cd ../backend && npm i

#       - name: Docker Setup
#         uses: docker/setup-buildx-action@v4.2.0

#       - name: Configure AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v6.1.0
#         with:
#           audience: sts.amazonaws.com
#           aws-region: ${{ secrets.AWS_REGION }}
#           role-to-assume: ${{ secrets.ROLE_TO_ASSUME }}
#           role-session-name: github-actions-session

#       - name: Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/amazon-ecr-login@v2

#       - name: Build, tag, and push docker FRONTEND image to Amazon ECR
#         env:
#           REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           REPOSITORY: multi-cicd-frontend-repo
#           IMAGE_TAG: ${{ github.sha }}
#         working-directory: app/frontend
#         run: |
#           docker build -t $REGISTRY/$REPOSITORY:$IMAGE_TAG .
#           docker push $REGISTRY/$REPOSITORY:$IMAGE_TAG

#       - name: Build, tag, and push docker BACKEND image to Amazon ECR
#         env:
#           REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           REPOSITORY: multi-cicd-backend-repo
#           IMAGE_TAG: ${{ github.sha }}
#         working-directory: app/backend
#         run: |
#           docker build -t $REGISTRY/$REPOSITORY:$IMAGE_TAG .
#           docker push $REGISTRY/$REPOSITORY:$IMAGE_TAG
